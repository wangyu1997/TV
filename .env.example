# 订阅源 URL，用英文逗号 "," 分隔
SUBSCRIPTION_URLS="xxx"

# (可选) 自定义缓存时间 (例如: 48)
CACHE_TIME="48"

# (可选) 站点 API 超时过滤 (单位: 毫秒)，若不设置则不过滤
TTL="500"

# (可选) GitHub Actions 更新频率 (Cron 表达式，例如: 每6小时一次)
# 将在 workflow 文件中直接使用，这里仅作说明
# UPDATE_SCHEDULE="0 */6 * * *"
